gearpump {
  serializers {
    "org.apache.gearpump.streaming.examples.pipeline.Messages$Datum" = ""
    "[Lorg.apache.gearpump.streaming.examples.pipeline.Messages$Datum;" = ""
  }
}

pipeline {
  cpu.interval = 10
  memory.interval = 20
  processors = 1
  persistors = 1
}

kafka {
    consumer {
      # consumer topics separated by ","
      topics = "demogate"
      fetch.threshold = 5000
      fetch.sleep.ms = 100
    }
  
    grouper.class = "org.apache.gearpump.streaming.kafka.lib.grouper.KafkaDefaultGrouper"
}

hbase {
  zookeeper.connect = "127.0.0.1"
  table {
    name = "pipeline"
    column {
      family = "metrics"
      name = "average"
    }
  }
}
